# VeilClaim runs on Preprod and reads its contract address from the committed deployments/preprod.json,
# so no configuration is needed to run the app. Set this only to point at a contract you deployed yourself.
VITE_CONTRACT_ADDRESS=
