VITE_NETWORK_ID=preprod
# Deployed VeilClaim contract address (64 hex chars). In dev, a contract deployed from the Dev tab overrides this.
VITE_CONTRACT_ADDRESS=
# Optional: indexer used to read the public ledger without a wallet. Defaults to the Midnight indexer for VITE_NETWORK_ID.
# VITE_INDEXER_URL=https://indexer.preprod.midnight.network/api/v4/graphql
# VITE_INDEXER_WS_URL=wss://indexer.preprod.midnight.network/api/v4/graphql/ws
