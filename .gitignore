# Private build brief
doc.md
docs
docs/
node_modules/
dist/
*.tsbuildinfo
*.log
coverage/
reports/
.vite/
.env
.env.local
.DS_Store
.idea/
midnight-level-db

# Compact compiler output is committed: the proving keys must match the verifier keys of the deployed contract.
# Recompiling can produce different keys, so do not regenerate it for an existing deployment.

# Dev-time copies of circuit assets
ui/public/keys/
ui/public/zkir/

# Demo provider signing key (bun run demo:issue)
.provider-secret

# E2E test wallet seeds (Preprod only)
e2e/.wallets.json
e2e/.state/
